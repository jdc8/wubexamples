# Add a listener on port 8080
Listener {
    -port 8080
}

# Simple Mason domain
/mason/ {
    domain Mason
    root docroot/mason
}

# Mason domain with authentication and filtering
/mason2/ {
    domain Mason
    root docroot/mason2
}
